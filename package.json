{
  "name": "opencode-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Opencode Orchestrator - SDK-driven pipeline for AI agent coordination",
  "scripts": {
    "dev:server": "pnpm --filter @orchestrator/server dev",
    "dev:web": "pnpm --filter @orchestrator/web dev",
    "dev": "pnpm run dev:server & pnpm run dev:web",
    "build:shared": "pnpm --filter @orchestrator/shared build",
    "build:server": "pnpm --filter @orchestrator/server build",
    "build:web": "pnpm --filter @orchestrator/web build",
    "build": "pnpm run build:shared && pnpm run build:server && pnpm run build:web",
    "test": "pnpm --filter @orchestrator/server test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
